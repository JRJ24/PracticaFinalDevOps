<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>¡Hola Mundo!</title>

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    :root{--bg1:#7b2ff7; --bg2:#5b8def; --card:#ffffffcc}
    html,body{height:100%}
    body{
      font-family: 'Poppins', system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
      background: linear-gradient(135deg,var(--bg1),var(--bg2));
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      color:#222;
    }
    .greeting-card{
      background: rgba(255,255,255,0.95);
      border-radius: 16px;
      padding: 2.25rem;
      box-shadow: 0 12px 30px rgba(10,10,30,0.25);
      max-width:860px;
      width:100%;
    }
    .title-gradient{
      font-size: clamp(1.6rem, 3.6vw, 3.5rem);
      font-weight:700;
      background: linear-gradient(90deg,#1f3bff,#7b2ff7,#ff8a71);
      background-clip: text;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom:0.25rem;
    }
    .subtitle{
      color:#556080;
      margin-bottom:1.25rem;
    }
    .wave {
      display:inline-block;
      transform-origin:70% 70%;
      animation: wave 1.6s ease-in-out infinite;
    }
    @keyframes wave{
      0%{transform:rotate(0deg)}
      15%{transform:rotate(14deg)}
      30%{transform:rotate(-8deg)}
      45%{transform:rotate(14deg)}
      60%{transform:rotate(-4deg)}
      75%{transform:rotate(10deg)}
      100%{transform:rotate(0deg)}
    }
    .feature-list li{margin-bottom:.5rem}
  </style>
</head>
<body>
  <div class="d-flex min-vh-100 align-items-center justify-content-center py-5">
    <div class="greeting-card">
      <div class="row align-items-center g-4">
        <div class="col-md-7">
          <h1 class="title-gradient">¡Hola, <span class="wave">👋</span> Mundo!</h1>
          <p class="subtitle">Una vista simple pero cuidada hecha con EJS y Bootstrap — perfecta para comenzar tu proyecto.</p>

          <ul class="feature-list list-unstyled">
            <li>• Responsive y centrado</li>
            <li>• Tipografía limpia (Poppins)</li>
            <li>• Colores modernos y accesibles</li>
          </ul>

          <div class="mt-4">
            <a href="#" class="btn btn-primary btn-lg me-2">Empezar</a>
            <a href="#" class="btn btn-outline-secondary btn-lg">Documentación</a>
          </div>
        </div>

        <div class="col-md-5 text-center d-none d-md-block">
          <svg width="220" height="220" viewBox="0 0 220 220" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <defs>
              <linearGradient id="g1" x1="0" x2="1">
                <stop offset="0" stop-color="#7b2ff7"/>
                <stop offset="1" stop-color="#ff8a71"/>
              </linearGradient>
            </defs>
            <rect x="10" y="10" width="200" height="200" rx="20" fill="url(#g1)" opacity="0.95" />
            <text x="50%" y="52%" dominant-baseline="middle" text-anchor="middle" font-size="36" font-weight="700" fill="rgba(255,255,255,0.95)">Hi</text>
          </svg>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap Bundle JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
